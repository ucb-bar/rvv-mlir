# rvv-mlir — SOL (Speed of Light), int8 W8A8 on the SpaceMiT K1

> **READ [`RESULTS.md`](RESULTS.md) FIRST.**
>
> **SOL means the CEILING, not a claim of reaching it.** This package records the roofline (int8
> compute roof 51.2 GMAC/s; per-model compulsory memory traffic), what ExecuTorch and merlin
> actually achieve against it, and the measured mechanisms that account for the difference — so
> analysis found by hand becomes a target the compiler can be measured against.
>
> **This configuration does NOT beat ExecuTorch on any model in the study**, and ExecuTorch is
> itself 7–45× off the compute roof. Measured ratios (ours/theirs, lower is better, 1.0 = parity):
> lstmnetvit **1.80×**, resnet50_v1_5 **3.37×**, tiny_llama **19.76×**.
>
> It is **not** the target's promoted champion — that remains
> [`stable/impr_tuned_wholemodel_vf_int8`](../../tree/stable/impr_tuned_wholemodel_vf_int8), whose
> `accumulator_resident_wholemodel_vf` feature this package does **not** carry. Whether this
> configuration regressed against it is queued and currently **UNKNOWN**.
>
> The boilerplate below is generated by the publish bridge; where it says "certified champion" it
> means "the package this branch exports", not that it won anything.

Standalone, buildable out-of-tree Merlin codegen backend for **rvv** (family `vector_schedule`).

This repository is **generated** by Merlin's `merlin-target-publish` bridge: it is the certified champion codegen package for the target, exported as its own repo. The buildable tree at the repo root *is* the content; the package manifest + provenance ride along under `.merlin/`.

## What

- Champion package: `sol_k1_int8`
- Family: `vector_schedule`
- Recorded status: `k1_verified`
- Merlin git sha (this export): `abe6cb3`

## How to build

```sh
git clone <this-repo> rvv-mlir
cd rvv-mlir
cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build
./build/bin/rvv-opt --version
```

## Provenance

- Certification: `recorded:k1_verified`
- Certified by run: `n/a`
- Certified against: **tier not recorded** — do not read this as an RTL result
- Fingerprint: `n/a`

See `.merlin/provenance.yaml` and `.merlin/certification.yaml` for the full lineage. Each commit on this repo is one promotion; the history is the provenance trail.
